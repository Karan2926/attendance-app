# Production build config for the React SPA.
# Build with:  vite build
# VITE_API_BASE sets the full API origin only if nginx is NOT proxying /api
# on the same origin. When nginx routes /api -> Django on the same domain,
# leave VITE_API_BASE empty (frontend and API share one origin).
VITE_API_BASE=
